services:
  aiostreams:
    image: ghcr.io/crunging/aiostreams:build-bitrate
    container_name: aiostreams
    restart: unless-stopped
    ports:
      - 3000:3000
    env_file:
      - .env
    volumes:
      - ./data:/app/data
